name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
#    outputs:
#      semVer: ${{ steps.gitversion.outputs.semVer }}

    steps:
    - uses: actions/checkout@v2

    - name: Unshallow Fetch
      run: git fetch --unshallow

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x

#    - name: Install GitVersion
#      uses: gittools/actions/gitversion/setup@v0.9.7
#      with:
#        versionSpec: '5.x'
#        includePrerelease: true
#
#    - name: Determine Version
#      id: gitversion
#      uses: gittools/actions/gitversion/execute@v0.9.7
#      with:
#        useConfigFile: true
#        configFilePath: .github/workflows/gitversion.yml
#        
    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Debug --no-restore Photino.NET/Photino.NET.csproj 

    - name: Pack Nuget Package
      run: mkdir ./nuget; dotnet pack --configuration Debug --no-restore --include-symbols --include-source --output ./nuget Photino.NET/Photino.NET.csproj

    - name: Set ApiKey
      run: nuget setApiKey ${{ secrets.GITHUB_TOKEN }} -src 'github' -ConfigFile ./.github/workflows/nuget.config
      
    - name: Publish
      run: cd ./nuget; nuget push *.nupkg -ApiKey ${{ secrets.GITHUB_TOKEN }} -src 'github' -ConfigFile ../.github/workflows/nuget.config 

#    - name: Publish to GitHub
#      uses: tanaka-takayoshi/nuget-publish-to-github-packages-action@v2.1
#      with:
#        nupkg-path:  './nuget/*.nupkg'
#        repo-owner:  ${{ github.actor }}
#        gh-user:  ${{ github.actor }}
#        token:  ${{ secrets.API_KEY }}
        